import React from 'react'
import { Form, Container, Title } from './HeroStyles' // Importando os estilos

const Hero: React.FC = () => (
  <Form>
    <Container>
      <Title>As melhores vagas para tecnologia, design e artes visuais.</Title>
    </Container>
  </Form>
)

export default Hero
